name: applicatiocandidature
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  curved_navigation_bar: ^1.0.6
  font_awesome_flutter: ^10.7.0
  fl_chart: ^0.68.0
  flutter_native_splash: ^2.4.1 # Ajout de la dépendance flutter_native_splash

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/splash.png # Ajout de l'image splash.png
    - assets/scope.png
    - assets/ellipse.jpeg

  uses-material-design: true
